use super::Node;
use super::Node::*;
use super::TruthTable;
use std::collections::BTreeSet;

impl Node {
    fn from_table2(table: &TruthTable, sum_of_products: bool) -> Box<Self> {
        
        Node::val(false)
    }
}